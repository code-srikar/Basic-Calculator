<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalculatorApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="digitBtn" parent="commonBtn">
        <item name="android:textColor">@color/BtnBackground</item>
    </style>

    <style name="operatorBtn" parent="commonBtn">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="commonBtn" parent="Widget.Material3.ExtendedFloatingActionButton.Icon.Primary">
        <item name="cornerRadius">36dp</item>
        <item name="android:layout_height">76dp</item>
        <item name="android:layout_width">76dp</item>
        <item name="backgroundTint">@color/black</item>
        <item name="android:layout_margin">12dp</item>
        <item name="android:textSize">32dp</item>
    </style>

</resources>